services:
  - type: web
    name: salary-app
    env: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: "3000"
      - key: PYTHONUNBUFFERED
        value: "1"
